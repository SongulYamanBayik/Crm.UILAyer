@model List<Crm.UILAyer.Models.RoleAssignViewModel>
@{
    ViewData["Title"] = "AssignRole2";
    Layout = "~/Views/Shared/_MemberLayout.cshtml";
}

<h1>Rol Atama Sayfası</h1>
<form method="post">
    @for (int i = 0; i < Model.Count; i++)
    {
        <div class="form-check">
            <input type="hidden" asp-for="@Model[i].RoleId" />
            <input type="hidden" asp-for="@Model[i].Name" />
            <input type="checkbox" asp-for="@Model[i].Exists" class="form-check-input" />
            <label class="form-check-label">@Model[i].Name</label>
        </div>
    }
    <button class="btn btn-info">Rol Düzenle</button>
</form>

